# The application itself is Flutter (see flutter_app/) and needs nothing
# from here. This covers only the Python tooling in scripts/:
# gcm_to_excel.py writes a workbook; build_clean_seed_db.py is stdlib only.
openpyxl>=3.0.10
